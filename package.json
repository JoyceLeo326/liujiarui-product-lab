{
  "name": "liujiarui-product-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --test && node --check app.js && node --check sw.js && node --check scripts/release-audit.mjs",
    "audit:live": "node scripts/release-audit.mjs"
  }
}
