{
  "name": "joyce-product-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "manifest:write": "node scripts/mirror-manifest.mjs --write",
    "manifest:check": "node scripts/mirror-manifest.mjs --check",
    "check": "npm run manifest:check && node --test && node --check app.js && node --check sw.js"
  }
}
